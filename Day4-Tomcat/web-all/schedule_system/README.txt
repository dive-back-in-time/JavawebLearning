完成了系统构建的第二期

第二期：
基本实现了登录与注册的前端界面
同时熟悉了由control层 —— service层 —— dao层的业务逻辑

第三期
实现了过滤功能逻辑,但是仅仅实现了逻辑
比如不能直接打开showSchedule.html页面，比较粗浅

第四期
前端输入用户名时，可以将输入数据与后端数据库进行校验，反馈结果
同时构建了相应结果的枚举用例，实现标准化，已变更性强，耦合低的代码逻辑。

存有一个问题：我注册名称重复了，注册提交后，数据库没有更新，是哪步阻止了这一操作？
